apiVersion: v1
kind: Service
metadata:
  name: {{ include "git-event-listener.fullname" . }}
  labels:
    {{- include "git-event-listener.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "git-event-listener.selectorLabels" . | nindent 4 }}
